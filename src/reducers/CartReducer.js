const initialState = {
    products: [],
    address: [],
    discount: 0,
    delivery: 0
}

export default (state = initialState, action) => {
    switch(action.type){
        
    }

    return state
}